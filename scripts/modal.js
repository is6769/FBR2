/**
 * Модальные окна - реализованы через чистый CSS (:target)
 *
 * Этот файл оставлен для совместимости.
 * Логика открытия/закрытия модалок работает через CSS-селектор :target
 * Дополнительная логика (Escape, клик вне) обрабатывается в main.js
 */

// Дополнительная функциональность для модальных окон уже в main.js
console.log('Модальные окна работают через CSS :target');
